Buffer()
~~~~~~~~ ["Buffer()" is deprecated. Use Buffer.from(), Buffer.alloc(), or Buffer.allocUnsafe() instead.]

new Buffer([])
~~~~~~~~~~~~~~  ["new Buffer([])" is deprecated. Use Buffer.from(), Buffer.alloc(), or Buffer.allocUnsafe() instead.]

Buffer.alloc()
